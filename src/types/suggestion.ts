export interface Suggestion {
    ids: string;
    pickup_address: string;
    drop_address: string;
    userId: string;
    userId_firstname: string;
    createdAt?: string;
    updatedAt?: string;
}